import type { NextPage } from "next";
import Head from "next/head";
import Index from "components/templates/Index";
import Button from "components/atoms/Button";

const Home: NextPage = () => {
  return <Button label={"aaa"} color={"secondary"} />;
};

export default Home;
